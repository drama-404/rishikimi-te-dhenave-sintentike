# Real survey exports — never commit real respondent data to a (possibly public) repo.
# (.xlsx = customer export; root-level .csv = real experts export. Synthetic CSVs
#  live under output/ and are intentionally NOT ignored.)
*.xlsx
/*.csv

# Local tooling / caches
.claude/
.vercel/
__pycache__/
*.pyc
